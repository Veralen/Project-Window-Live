<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
    <UseWPF>true</UseWPF>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AssemblyName>WindowLive</AssemblyName>
    <ApplicationIcon>Assets\WindowLive.ico</ApplicationIcon>
    <!-- The shipped llama-server-cuda.exe / CUDA runtime DLLs are win-x64 only. -->
    <PlatformTarget>x64</PlatformTarget>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\WindowLive.Core\WindowLive.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- WPF pack resource so BuildTrayIconGraphic can load the 32px frame at runtime. -->
    <Resource Include="Assets\WindowLive.ico" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="H.NotifyIcon.Wpf" Version="2.2.0" />
    <PackageReference Include="TesseractOCR" Version="5.5.2" />
  </ItemGroup>
</Project>
