<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:WindowLive.App.Ui">

    <!--
      Tray context-menu chrome, design_handoff_project_window_1b section 4.

      Deliberately scoped via x:Key rather than an implicit (TargetType-only)
      style so this ONLY affects controls the app code explicitly opts in —
      SettingsWindow's buttons/menus and any other WPF menu in the app are
      untouched. Colors/fonts are pulled from Theme.cs (x:Static) so this
      dictionary has no duplicated hex literals.

      Keys (App.xaml.cs wires these onto the tray's ContextMenu/MenuItem/
      Separator instances — not done here):
        TrayContextMenu   - apply to the tray's ContextMenu
        TrayMenuItem      - apply to each top-level MenuItem added to it
        TrayMenuSeparator - apply to each Separator added to it

      The default MenuItem chrome (icon/checkmark gutter, submenu arrow,
      Win11 popup shadow/animation) is fully replaced by a flat Border+Grid
      template — there is no icon/check column to suppress because it was
      never built.

      Corner radius 4px here is the one documented exception to the
      app-wide radius-0 rule (see Theme.cs remarks).
    -->

    <Style x:Key="TrayContextMenu" TargetType="ContextMenu">
        <Setter Property="Background" Value="{x:Static ui:Theme.PopupBg}"/>
        <Setter Property="BorderBrush" Value="{x:Static ui:Theme.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontFamily" Value="{x:Static ui:Theme.UiFontFamily}"/>
        <Setter Property="HasDropShadow" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayMenuItem" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{x:Static ui:Theme.TextTray}"/>
        <Setter Property="FontFamily" Value="{x:Static ui:Theme.UiFontFamily}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0"
                                               ContentSource="Header"
                                               VerticalAlignment="Center"
                                               TextElement.Foreground="{TemplateBinding Foreground}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{TemplateBinding InputGestureText}"
                                       FontFamily="{x:Static ui:Theme.MonoFontFamily}"
                                       FontSize="9.5"
                                       Foreground="{x:Static ui:Theme.TextMuted}"
                                       VerticalAlignment="Center"
                                       Margin="18,0,0,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{x:Static ui:Theme.HoverBg}"/>
                            <Setter Property="Foreground" Value="{x:Static ui:Theme.TextPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{x:Static ui:Theme.TextMuted}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayMenuSeparator" TargetType="Separator">
        <Setter Property="Background" Value="{x:Static ui:Theme.Divider}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="6,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
